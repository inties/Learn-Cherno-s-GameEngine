
目前主循环每一帧遍历所有层并更新，例如渲染层，imgui层，通过windows发出事件添加到事件队列中，然后在主循环中调用事件处理函数，并事件传递到每层

事件回调 不是直接在主循环中执行的 ，而是由GLFW库在后台管理的。GLFW使用事件驱动模型：

- 当操作系统产生事件（如鼠标点击、键盘输入）时，GLFW会将它们放入事件队列。
- 在主循环中，通过调用 glfwPollEvents() （通常在 m_Window->Update() 内部实现）来轮询并处理队列中的事件。这会触发已注册的回调函数。